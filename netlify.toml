[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Kit generation runs 4 LLM calls in parallel — give the function room.
[functions]
  node_bundler = "esbuild"
  included_files = []

[functions."api/jobs/[id]/kit"]
  timeout = 60
